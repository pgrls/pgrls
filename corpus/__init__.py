"""pgrls adjudicated precision corpus.

A labeled set of small, self-contained schemas — positives that must
fire a specific rule and adversarial negatives that must stay silent —
used to measure per-rule precision / false-positive behavior over the
real lint path. See :mod:`corpus.harness` and ``docs/PRECISION.md``.
"""
