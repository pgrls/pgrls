-- Remediation SQL generated by pgrls 0.55.0.
--
-- Review every statement before applying. Generated from
-- a snapshot of the database; regenerate if the schema
-- has changed since. 2 fixes.

-- [SEC015] Pin pg_temp as the last entry of s15.f_none()'s search_path so Postgres searches it last for relation / type names, blocking the pg_temp shadowing escalation path. Preserves the existing search_path entries and pins pg_temp at the end (any earlier pg_temp occurrences are stripped so pg_temp is searched LAST, exactly once). Other overloads of the same function are remediated separately by their own Fix. If the function body fully-qualifies every object reference (search_path is moot), keep it and allowlist 's15.f_none' in [lint.rules.SEC015].
ALTER FUNCTION s15.f_none() SET search_path = s15, public, pg_temp;

-- [SEC015] Pin pg_temp as the last entry of s15.f_one(a integer)'s search_path so Postgres searches it last for relation / type names, blocking the pg_temp shadowing escalation path. Preserves the existing search_path entries and pins pg_temp at the end (any earlier pg_temp occurrences are stripped so pg_temp is searched LAST, exactly once). Other overloads of the same function are remediated separately by their own Fix. If the function body fully-qualifies every object reference (search_path is moot), keep it and allowlist 's15.f_one' in [lint.rules.SEC015].
ALTER FUNCTION s15.f_one(a integer) SET search_path = s15, public, pg_temp;
